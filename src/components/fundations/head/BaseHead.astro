---
import Seo from "@/components/fundations/head/Seo.astro";
import Fonts from "@/components/fundations/head/Fonts.astro";
import Favicons from "@/components/fundations/head/Favicons.astro";

interface Props {
  title: string;
  description: string;
  image?: string;
  noindex?: boolean;
  structuredData?: Record<string, unknown>;
}

const {
  title,
  description,
  image = "/og/sobhi-el-safady.png",
  noindex = false,
  structuredData,
} = Astro.props;
---

<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<meta name="theme-color" content="#020710" />
<meta name="color-scheme" content="dark" />
<Seo
  title={title}
  description={description}
  image={image}
  noindex={noindex}
  structuredData={structuredData}
/>
<Fonts />
<Favicons />
